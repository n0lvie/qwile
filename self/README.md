# self/

This directory is Qwile's memory — everything that **WAS**.

It is created automatically on first birth and grows throughout Qwile's life. It is **immutable by design** — experiences are only added, never erased.

This directory should be empty in the repository. Each instance of Qwile builds its own unique history.
